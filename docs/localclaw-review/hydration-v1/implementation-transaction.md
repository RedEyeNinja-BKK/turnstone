# Implementation Transaction — Hydration v1 (Final Staged Order)

Every mutation is content/governance via native mechanisms; **no custom code, no package-core change, no new subsystem**. Each step: `action → expected effect → verification → stop condition → rollback`. Steps execute sequentially; each must be verified and reversible before the next.

| Step | Action | Expected effect | Verification | Stop condition | Rollback |
|---|---|---|---|---|---|
| 1 | **Capture fresh representative Turnstone-session PRE-mutation baseline** (primary evidence): spawn a fresh Turnstone session/workstream with the **current** persona + memory index; run cases 1–7, record retry counts, static prefix, preflight retrieval cost, effective cost. Existing task_agent baseline becomes **secondary evidence**. Rollback inputs verified | Primary baseline file exists (cases 1–7 + retries + static/preflight/effective measurements); manifest schema ready; memory index fully enumerated read-only | Missing baseline/rollback input → do not proceed | n/a (read-only) |
| 2 | Create stable-baseline manifest (`<LOCAL_SHARED_WORKSPACE_PATH>/operations/stable-baseline.md`) | Pointer layer exists; no duplicated state; **no active-task required inputs, no duplicated checkpoint inventory** (pointer/query only) | File present; each entry has source→pointer→freshness→retrieval; pointer resolves | Pointer resolution fails → fix manifest before proceeding | Delete file |
| 3 | Reduce global prompt-policy injection — **one domain policy at a time** (6 domain policies → disable; keep 3 universal) | 9 → 3 globally injected policies, each domain policy individually proven before the next is disabled | **Per-policy sequence**: (a) disable exactly ONE domain policy; (b) spawn a fresh session with that policy's destination persona and prove the required behavior remains (activation-path proof, not text existence); (c) spawn an unrelated fresh session and prove the domain policy is **absent** from its prompt; (d) on success, proceed to the next policy; on any failure, **immediately re-enable that policy and STOP** for that policy before continuing anything else. Final: fresh-session prompt dump shows only the 3 universal policies; battery cases 1–3 PASS | Any per-policy activation/absence proof fails → re-enable that policy immediately, stop for that policy, do not disable further policies | Re-enable disabled policy (admin API, byte-identical) |
| 4 | **Retire Core Identity skill (Option 3) — disable/deactivate, NOT delete** (exact mechanism: native skills surface — set `enabled=false` and `is_default=false` via admin skill update; the skill row is **retained** in storage, hidden from `find` (unless `enabled_only=false`), and rejected by `load`/`spawn_workstream`); add session-start sequence to `identity/working-rules.md` | No Core Identity injection; session-start preserved in working-rules; skill row retained for reversible disable | Fresh-session prompt has no Core Identity content; working-rules contains session-start sequence; kernel doctrine intact; battery cases 1–5 PASS | Kernel doctrine lost → re-enable skill | Re-enable skill (native `enable`, row retained byte-identical — no content change). Deletion is NOT used in v1; if a later phase ever deletes the row, rollback becomes semantic/state-equivalent with non-restorable metadata (skill_id, created/updated timestamps, version/approval history) |
| 5 | Trim persona to minimum kernel (per `turnstone-kernel.proposed.md`) | Persona ≈ 23.2KB → ≈ 13KB; kernel complete | Persona bytes measured; kernel checklist complete; battery cases 1–5 PASS | Any kernel element missing → restore prior persona | Restore prior persona content (byte-identical) |
| 6 | Generate + freeze private memory restoration manifest + exact keep/retire set (read-only) | Full inventory classified; **every retired candidate has a verified archival retrieval anchor OR is fully recoverable from the manifest**; SHA256 of private candidate set recorded | Manifest validates; per-candidate anchor/restorability check passes; counts reported (keep/retire are **estimates, not quotas**); SHA recorded | Any candidate lacks anchor/restorability → fix or keep | n/a (read-only; manifest is additive) |
| 7 | Operator review of exact keep/retire set | Vincent approves exact set before any deletion | Written GO/approval recorded | No GO → no deletion | n/a |
| 8 | Memory-retirement canary (5–10 low-risk entries, all with verified anchors) | Small, reversible retirement | Index prefix reduction measured; archived-fact retrieval via manifest→evidence succeeds | Retrieval fails → restore canary, stop | Restore canary from manifest (semantic/state-equivalent) |
| 9 | Prove prefix reduction | Measured reduction ≈ expected per batch | Index bytes/tokens re-measured | No reduction / wrong reduction → stop | n/a (measurement) |
| 10 | Prove archive retrieval | Retired fact retrievable via native path | Retrieval probe succeeds for ≥1 retired canary | Retrieval fails → restore canary, stop | Restore canary |
| 11 | Restore ≥1 retired canary; verify semantic equivalence | Restoration works; state-equivalent | Re-saved entry present with same name/content/description/type/scope/scope_id | Restore fails → stop, investigate manifest schema | n/a (this IS the rollback proof) |
| 12 | Quality spot-check (fresh session) | No architecture/authority/evidence regression | **Fresh Turnstone session** battery cases 1, 4, 6 PASS | Any regression → restore canary, stop | Restore canary |
| 13 | Bounded bulk retirement (batches ≤100) | Retire approved set incrementally | Manifest appended per batch (before delete); index re-measured; sample retrieval; spot-check | Any mismatch → stop, reconcile, roll back batch | Restore batch from manifest |
| 14 | Verification after every batch | Every batch independently verified | Same verification as step 13 | Any failure → stop | Restore failed batch |
| 15 | **Full quality battery in a fresh Turnstone session** (7 cases) + independent lanes | No regression post-retirement | All 7 PASS in fresh session (with retry handling); task_agent/Hermes as independent review lanes | Any FAIL → roll back offending step | Per-step rollback |
| 16 | **Final effective-cost measurement** | Static prefix ≈ 21–24K; effective (static + preflight) ≤ 26–28K | Measured static + preflight matches targets; report table | Target not met → report, do not force | n/a (measurement) |
| 17 | Reconciliation / closure | Manifest hash recorded; ledger entries; report saved | Chain verifies; evidence files present; quality baseline updated | Reconciliation mismatch → resolve before closure | n/a |

## Global boundaries (every step)

- **No production mutation before operator GO** on the exact approved transaction and, at step 7, on the exact keep/retire set.
- No package-core, vector DB, memory daemon, new MCP server, new persistence layer, new callable subsystem, Switchyard change, FGV1 reopen, provider/model/Qwen/Comfy/Anthropic qualification, or economics work.
- Fleet Governance v1 and Switchyard S2-H remain closed baselines; PR #7 and Switchyard PR #10 remain untouched.
- No service restarts required for any step; full revert leaves the platform byte-identical except memory rows (semantic/state-equivalent restoration by design).
